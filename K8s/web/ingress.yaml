apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-ingress
spec:

  rules:
  - host: shebl.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web
            port:
              number: 8080
